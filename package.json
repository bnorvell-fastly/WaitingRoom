{
  "type": "module",
  "dependencies": {
    "@fastly/js-compute": "^3.33.1",
    "@upstash/redis": "^1.34.4",
    "jose": "^5.10.0",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "@fastly/cli": "^10.14.0"
  },
  "scripts": {
    "build": "js-compute-runtime --enable-aot ./src/index.js ./bin/main.wasm",
    "start": "fastly compute serve",
    "deploy": "fastly compute publish"
  }
}
